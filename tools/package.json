{
  "name": "cibseven-remote-example-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the CIB seven remote bike-leasing blueprint: BPMN linting (bpmnlint). Run from the repo root via `npm --prefix tools run <script>`.",
  "scripts": {
    "lint:bpmn": "bpmnlint \"../service/example-service/src/main/resources/bpmn/*.bpmn\""
  },
  "devDependencies": {
    "bpmnlint": "11.12.1"
  }
}
